/**
 * Kept as a stable import path — the implementation now lives in the shared
 * motion kit alongside CountUp, Marquee, Spotlight and friends.
 */
export { Reveal, Stagger } from '@/components/motion'
